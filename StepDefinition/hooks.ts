import { Before, After, Status, BeforeAll } from "cucumber";
import { browser } from "protractor";
import { HookScenarioResult } from 'cucumber';
//import testData from "../TestData/Properties";

 /*After(async function (scenario){
console.log("Test is Completed. Lets see the Report");
if(scenario.result.status==='failed')
{
  const prtscrn=await browser.takeScreenshot();
  await this.attach(prtscrn, "image/png");
}

});  */



